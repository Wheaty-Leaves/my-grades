<%= form_for ( [@course, @enrolment] ) do |form| %>
  <% if enrolment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(enrolment.errors.count, "error") %> prohibited this enrolment from being saved:</h2>

      <ul>
        <% enrolment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.collection_select :student_id, @unenrolled_students, :id, :uniID %>
    <%= form.submit "Add Student", :class => "mt-3 btn btn-primary" %>
  </div>
<% end %>
